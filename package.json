{
  "name": "morocco-tours",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "npm --prefix apps/web run build && node scripts/copy-vercel-output.mjs",
    "dev:web": "npm --prefix apps/web run dev"
  }
}
