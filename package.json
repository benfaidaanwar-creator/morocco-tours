{
  "name": "morocco-tours",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "prebuild": "npm --prefix apps/web install --legacy-peer-deps",
    "build": "npm --prefix apps/web run build",
    "dev:web": "npm --prefix apps/web run dev"
  }
}
